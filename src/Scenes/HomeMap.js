import { imgUrl, soundUrl, lottieUrl } from "../utils/path"

const HomeMap = {
  id: "select",

  Bg: `${imgUrl}Intro_Bg.svg`,

  sprites: [
    `${imgUrl}play.svg`
  ],


  sounds: [
    // `${soundUrl}Intro.mp3`
  ],

  lottie: [

  ]
}

export default HomeMap;