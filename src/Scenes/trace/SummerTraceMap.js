import { imgUrl, soundUrl, lottieUrl } from "../../utils/path"

const SummerMap = {
  id: "summertrace",

  Bg: `${imgUrl}Dress-room.svg`,

  sprites: [
    `${imgUrl}Boy.svg`,
    `${imgUrl}SummerCap.svg`,
    `${imgUrl}SummerShirt.svg`,
    `${imgUrl}SummerPant.svg`
  ],


  sounds: [],

  lottie: []
}

export default SummerMap;